# Faster Races

**Unreleased:** Lifecycle hardening update — expected quit/menu teardown is now logged cleanly and Harmony patches are unpatched on destroy. Reflection fallback diagnostics for race-name reads now use one-time BepInEx debug logging instead of `Debug.WriteLine`.

**Version 1.4.0** — Adds a **configurable movement speed bonus** for your character. When used with **Haven's Birthright**, this mod suppresses Haven's Birthright movement speed bonuses only while Faster Races speed bonus is active, so you do not get double speed.
